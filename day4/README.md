```zsh
# If running from the project's root directory:
python -m day4.word_search --debug xmas day4/test_input.txt

# If running from within the `day4` directory:
PYTHONPATH=.. python -m word_search --debug xmas test_input.txt
```

# Results

```zsh
$ PYTHONPATH=.. python -m x_search --debug sxmas input.txt
$ # ...
"SXMAS" can be found 9 times in the shape of an "X".
```
